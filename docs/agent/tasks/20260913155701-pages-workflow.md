+++
schema_version = 1
id = "TASK-20260913155701-pages-workflow"
kind = "task"
status = "completed"
task_level = "high_risk"
created_at = "2026-09-13T15:56:59.2408460Z"
updated_at = "2026-09-13T16:04:37.199612+00:00"
parent = "PLAN-20260913155549-repository-presence-and-landing"
repositories = ["root"]
protected_areas = ["authorization", "release"]

[approval]
approved_by = "workspace-user"
approved_at = "2026-09-13T15:56:59.2408460Z"
evidence = "approved repository presence and landing implementation plan"
+++

# Task: GitHub Pages workflow

## Goal

Add the approved least-privilege GitHub Pages deployment workflow with verified action pins.

## Write scope

`.github/workflows/pages.yml` and workflow-focused tests/documentation if required.

## Acceptance criteria

The workflow deploys only `site/`, pins official actions to verified SHAs, and uses minimal required
permissions and concurrency.

## Verification

Workflow inspection, pinned-SHA tests, full tests, validator, and GitHub deployment run.

## Completion evidence

Verified official release tags and immutable SHAs for configure-pages v6.0.0,
upload-pages-artifact v5.0.0, and deploy-pages v5.0.1. Added a least-privilege workflow deploying
only `site/` and a pinned-action regression test. Full suite passes 43/43, validator reports 0/0,
and diff check passes.

