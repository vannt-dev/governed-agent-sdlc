# Polyrepo example

This manifest demonstrates two project repositories mounted beneath one coordination workspace.
Create `repositories/backend` and `repositories/frontend`, or change the paths to match the real
workspace. Repository IDs, never hard-coded product paths, are used by artifact metadata.

